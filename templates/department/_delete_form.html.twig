<form method="post" action="{{ path('app_department_delete', {'id': department.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ department.id) }}">
    <button type="button" class="dropdown-item text-danger delete-record">Delete</button>
</form>
